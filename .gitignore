# โมเดล Vosk ขนาด 88MB — โค้ดโหลดจาก GitHub Release ตอน runtime อยู่แล้ว
# (ดู VOSK_MODEL_URL ใน script.js) จึงไม่ต้อง deploy ไปด้วย
vosk-model/

# คลิปภาษามือ — ไฟล์บีบอัดแล้วใน signs/*.mp4 (~27MB รวม 242 ไฟล์) เล็กพอ
# commit ตรงลง repo ได้เลย ไม่ต้องพึ่ง GitHub Release asset แบบไฟล์รวมเดิม
# ส่วนต้นฉบับคุณภาพเต็ม (2.4GB, HEVC) เก็บไว้อ้างอิงเฉย ๆ ไม่ deploy/commit
signs/คลิปภาษามือ/

# ไฟล์เปล่าที่ค้างอยู่
video

# เครื่องมือ dev
.claude/
node_modules/

# tools/gesture-extract: debug output ซ้ำกับ signGestureVocab.js ที่ commit
# อยู่แล้ว ไม่ต้อง commit ไฟล์ raw/สแนปช็อตดีบักด้วย
tools/gesture-extract/gesture-refs.json
tools/gesture-extract/debug-frame.png

# ผลลัพธ์จาก Bubblewrap (สร้างใหม่ได้ และมี keystore ห้าม commit เด็ดขาด)
twa/
*.keystore
*.jks
